-- Script which updates the score of Bob to 10.
-- The use of Bob's id value is prohibited, only the name field is allowed
-- mysql is passed as database name as argument of mysql command

UPDATE second_table SET score=10 WHERE name="Bob";
